import { Text, View } from "react-native";

export default function ConfiguracionScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Perfil - Configuración</Text>
    </View>
  );
}
