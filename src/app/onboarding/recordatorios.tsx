import { Text, View } from "react-native";

export default function RecordatoriosScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Onboarding - Recordatorios</Text>
    </View>
  );
}
